const HelloBar = () => {
  return (
    <div className="h-[55px] w-full bg-emerald-600 flex items-center justify-center">
      {/* Contenuto da aggiungere */}
    </div>
  );
};

export default HelloBar;
