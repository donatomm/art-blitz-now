const HelloBar = () => {
  return (
    <div className="fixed top-16 left-0 right-0 z-40 h-[30px] w-full bg-emerald-600 flex items-center justify-center">
      {/* Contenuto da aggiungere */}
    </div>
  );
};

export default HelloBar;
